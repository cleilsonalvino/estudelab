const htmlContent = `
<h2>10. Tags de Input</h2>

<h3>O que são Tags de Input?</h3>
<p>A tag <code>&lt;input&gt;</code> em HTML é usada para criar campos de entrada que permitem que os usuários insiram dados em um formulário. Dependendo do tipo especificado, os campos de entrada podem aceitar texto, senhas, botões e muito mais.</p>

<h3>Tipos Comuns de Input</h3>
<p>A tag <code>&lt;input&gt;</code> pode ter vários tipos, incluindo:</p>

<ul>
    <li><strong>&lt;input type="text"&gt;</strong> - Campo de texto para entrada de dados simples.</li>
    <li><strong>&lt;input type="password"&gt;</strong> - Campo para entrada de senhas, onde os caracteres são ocultos.</li>
    <li><strong>&lt;input type="email"&gt;</strong> - Campo para entrada de endereços de email, com validação básica.</li>
    <li><strong>&lt;input type="checkbox"&gt;</strong> - Permite que os usuários selecionem ou desmarquem uma opção.</li>
    <li><strong>&lt;input type="radio"&gt;</strong> - Permite a seleção de uma única opção de um grupo de opções.</li>
    <li><strong>&lt;input type="file"&gt;</strong> - Permite o upload de arquivos.</li>
</ul>

<h3>Exemplos de Uso</h3>
<p>A seguir, exemplos práticos de alguns tipos de <code>&lt;input&gt;</code> mencionados:</p>

<h3>Atributos Comuns</h3>
<p>Os campos de entrada podem incluir vários atributos para personalização, como:</p>

<ul>
    <li><strong>type</strong> - Define o tipo de entrada (ex: <code>text</code>, <code>password</code>, <code>email</code>).</li>
    <li><strong>placeholder</strong> - Fornece uma dica de texto que aparece dentro do campo quando vazio.</li>
    <li><strong>required</strong> - Indica que um campo deve ser preenchido antes do envio do formulário.</li>
    <li><strong>disabled</strong> - Desativa um campo de entrada, tornando-o não interativo.</li>
    <li><strong>maxlength</strong> - Define o número máximo de caracteres que podem ser inseridos.</li>
    <li><strong>minlength</strong> - Define o número mínimo de caracteres que devem ser inseridos.</li>
</ul>
</ul>

`

export default htmlContent;